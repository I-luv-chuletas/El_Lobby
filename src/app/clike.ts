export class Clike {

  constructor(values: Object = {}) {
    Object.assign(this, values);
  }

  id: string;
  userID: string;
  commentID: string;
  comId: string;
  createdAt: number;
  updatedAt: string;
}
